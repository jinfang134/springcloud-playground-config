
message: hello test message

spring:
  boot:
    admin:
      client:
        url: http://localhost:8000

# eureka:
#   client:
#     serviceUrl:
#       defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: ALWAYS
